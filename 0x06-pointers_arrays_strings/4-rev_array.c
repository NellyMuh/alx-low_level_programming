#include "main.h"

/*
 * rev_aray - prints an array in reverse
 *@a: array of integers.
 *@n: the number of elements to swap
 * Retun : Always success
 **/

void reverse_array(int *a, int n)
{
	int i, a[];
	
	for ( i = n-1; i > 0; i--)
	{
		printf("the reverse array is: %d\n", a[i]);
	}	
	return (0);
}
